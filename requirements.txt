numpy>=1.24
pandas>=2.0
torch>=2.0
scikit-learn>=1.3
scipy>=1.11
pyarrow>=12.0
joblib>=1.3
yfinance>=0.2

# Faz 1 — Regime detection (HMM + viz)
hmmlearn>=0.3
matplotlib>=3.7
statsmodels>=0.14   # ADF testi (fracdiff.find_min_d)
asyncpg>=0.29       # PostgreSQL async driver (engine/data/db/)

# Faz 3 — Bayesian meta-optimization
optuna>=3.5

# API — FastAPI SPA backend
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0
python-multipart>=0.0.9

# Faz 6 — Orchestration (Prefect 3.x)
prefect>=3.0,<4.0

# v4 Faz 1 — Performans Katmanı (Oracle 64GB üzerinde 10× hızlanma)
duckdb>=0.10        # wf_fitness vektörize IC (Faz 1.2)
polars>=0.20        # factor_neutralize hızlı groupby (Faz 1.3)
numba>=0.59         # alpha_cfg operatör kernel'leri (Faz 1.4)
redis>=5.0          # Redis Streams pub/sub (Faz 1.5)
